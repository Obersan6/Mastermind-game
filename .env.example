# .env.example
# Copy to .env and fill in real values.
# NEVER commit your actual .env.

# Flask
SECRET_KEY=changeme
APP_ENV=dev   # dev | test | prod

# Database (SQLAlchemy psycopg3-style URI)
# Replace <username>, <password>, and <dbname>. Adjust host/port if needed.
DATABASE_URL=postgresql+psycopg://<username>:<password>@localhost:5432/<dbname>
